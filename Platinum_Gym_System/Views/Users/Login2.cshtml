@model User
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ingreso de Miembro</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="~/css/Login2StyleSheet.css" />
</head>

<body>
    <div class="login-container">

        <!-- Mensaje de Error -->
        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger">
                <i class="fas fa-exclamation-triangle me-2"></i>@ViewBag.Error
            </div>
        }

        <!-- Tarjeta de Login -->
        <div class="login-card">

            <div class="card-header">
                <div class="brand">
                    <div class="brand-icon">
                        <i class="fas fa-id-card-alt"></i>
                    </div>
                    <h4 class="mb-0">Control de Miembros</h4>
                </div>

                <h1 class="card-title">Inicio de Sesión</h1>
                <p class="card-subtitle">Ingresa tus credenciales para registrarte</p>
            </div>

            <div class="card-body">

                <form asp-action="Login2">

                    <!-- CI -->
                    <div class="form-group">
                        <label class="form-label" asp-for="CI">
                            <strong>CI (Carnet de Identidad)</strong>
                        </label>
                        <input type="text" class="form-control" asp-for="CI" placeholder="Ingresa tu número de carnet" />

                        <div class="info-text">
                            Introduce tu número de identificación para continuar
                        </div>
                    </div>

                    <!-- Password -->
                    <div class="form-group">
                        <label class="form-label" asp-for="Password">
                            <strong>Contraseña</strong>
                        </label>

                        <div class="input-group">
                            <input type="password" class="form-control" id="password" asp-for="Password" placeholder="Ingresa tu contraseña" />
                            <button type="button" class="password-toggle" id="togglePassword">
                                <i class="fas fa-eye"></i>
                            </button>
                        </div>

                        <div class="info-text">
                            Introduce la contraseña asociada a tu cuenta
                        </div>
                    </div>

                    <!-- Botón -->
                    <button type="submit" class="btn-login">
                        <i class="fas fa-sign-in-alt me-2"></i>Ingresar
                    </button>

                </form>
            </div>

        </div>

        <div class="footer">
            <p>&copy; 2023 Sistema de Control de Miembros. Todos los derechos reservados.</p>
        </div>
    </div>

    <script>
        document.getElementById('togglePassword').addEventListener('click', function () {
            const passwordInput = document.getElementById('password');
            const icon = this.querySelector('i');

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        });

        const inputs = document.querySelectorAll('.form-control');
        inputs.forEach(input => {
            input.addEventListener('focus', function () {
                this.parentElement.classList.add('focused');
            });

            input.addEventListener('blur', function () {
                if (this.value === '') {
                    this.parentElement.classList.remove('focused');
                }
            });
        });
    </script>

</body>
</html>
